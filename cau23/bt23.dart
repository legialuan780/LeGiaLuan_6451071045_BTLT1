// Tạo enum cho giới tính
enum Gender { male, female, others }

void main() {
  // In tất cả giá trị của enum
  for (var gender in Gender.values) {
    print(gender);
  }
  print("MSSV:6451071045");
}